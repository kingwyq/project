string类型直接赋值字符串，与赋值new string（“”）的区别：
直接赋值，若所赋值字符串不存在于常量池中则在常量池中创建该字符串对象，如果存在则直接指向该已存在字符串的地址。
注意：java中用==来比较两个对象时，比较的是它们的地址值，而用equals比较两个对象才是比较对象的值
若采用new string的方式赋值时，无论该字符串是否存在，都会在堆中创建字符串对象，而如果字符串不存在于字符串常量池中则会在字符串常量池中创建该字符串对象
如果存在则不需要在字符串常量池中创建。
